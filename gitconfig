[push]
	default = simple
[alias]

	# View abbreviated SHA, description, and history graph of the latest 20 commits
	k = log --pretty=oneline -n 20 --graph --abbrev-commit
	s = status
[user]
	email = scozv@yandex.com
	# config user on each terminal
	name = scott@ubuntu
